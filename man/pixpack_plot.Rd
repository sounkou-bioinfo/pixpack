% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Rpixpack.R
\name{pixpack_plot}
\alias{pixpack_plot}
\title{Visualize PixPack PNG metadata}
\usage{
pixpack_plot(png_path, show_grid = TRUE)
}
\arguments{
\item{png_path}{Path to a PixPack PNG file}

\item{show_grid}{Logical; if TRUE, overlays a grid showing the data cells}
}
\value{
Invisibly returns a list with metadata (if extractable)
}
\description{
Creates a simple visualization showing the grid structure and metadata
of a PixPack PNG file.
}
\examples{
\dontrun{
# Create a test PNG
test_text <- "Hello, PixPack!"
png_file <- tempfile(fileext = ".png")
pixpack_text(test_text, png_file)

# Visualize it
pixpack_plot(png_file)

# Clean up
unlink(png_file)
}
}
