% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Rpixpack.R
\name{pixpack_text}
\alias{pixpack_text}
\title{Encode text to PNG or decode PNG to text}
\usage{
pixpack_text(text = "", png_path, verbose = TRUE)
}
\arguments{
\item{text}{Character string to encode (ignored if png_path exists and is a PixPack PNG)}

\item{png_path}{Path where the PNG should be saved or read from}

\item{verbose}{Logical; if TRUE, prints status messages}
}
\value{
If encoding: path to created PNG. If decoding: the decoded text.
}
\description{
User-friendly wrapper for encoding strings to PNG images or decoding them back.
}
\examples{
\dontrun{
# Encode text to PNG
png_file <- tempfile(fileext = ".png")
result <- pixpack_text(text = "Hello, PixPack!", png_path = png_file)

# Decode PNG back to text
decoded_text <- pixpack_text(png_path = png_file)
print(decoded_text) # Should print "Hello, PixPack!"

# Clean up
unlink(png_file)
}
}
