% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/000-wrappers.R
\name{StringConversion}
\alias{StringConversion}
\title{Encode a string into a PixPack PNG at given path or decode an existing PixPack PNG to string.
If \code{png_path} already exists and is a PixPack PNG, it is decoded; otherwise \code{input} is encoded to that PNG path.}
\usage{
StringConversion(input, png_path)
}
\arguments{
\item{input}{Character scalar (content when encoding; ignored on decode).}

\item{png_path}{Target PNG path (must be provided).}
}
\value{
On encode: PNG path; On decode: decoded UTF-8 string (lossy).
}
\description{
Encode a string into a PixPack PNG at given path or decode an existing PixPack PNG to string.
If \code{png_path} already exists and is a PixPack PNG, it is decoded; otherwise \code{input} is encoded to that PNG path.
}
